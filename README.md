# Class-Student-Database

- Allows the user to view a specific class distribution.
- User can customize the class distribution
- Can upload JSON to create table of class distribution
- Can export class distributions as a JSON
